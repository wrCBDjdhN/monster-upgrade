# 数据库文件（运行时自动生成）
db/game.db
# 编译缓存
*.pyc
__pycache__/
# 虚拟环境
.venv/
# 工作目录
.sisyphus/
# 工作目录（opencode 工具）
.omo/
# 打包产物（PyInstaller 的 dist/build、Inno Setup 安装包输出）
dist/
build/
# 项目知识库（本地维护，不提交）
AGENTS.md
# opencode配置（本地维护，不提交）
opencode.json
